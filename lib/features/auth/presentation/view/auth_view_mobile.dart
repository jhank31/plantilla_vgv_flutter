import 'package:flutter/material.dart';

class AuthViewMobile extends StatelessWidget {
   
  const AuthViewMobile({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('AuthViewMobile'),
      ),
    );
  }
}
