extension Rsponsive on num {
  // sufijo para las medidas de ancho/width en la UI de la app
  double get w => toDouble();
  // sufijo para las medidas de alto/height en la UI de la app
  double get h => toDouble();
}
